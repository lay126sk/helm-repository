apiVersion: v1
kind: Service
metadata:
  name: {{ template "ntm-helm-test.fullname" . }}
  labels:
    app: {{ template "ntm-helm-test.name" . }}
    chart: {{ template "ntm-helm-test.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "ntm-helm-test.name" . }}
    release: {{ .Release.Name }}
